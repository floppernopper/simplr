<% if @location %> // if location set successfully
  if ($("#user_location").length) {
    $("#user_location").replaceWith("<%=j render 'settings/user_location' %>");
  } else {
    $("#user_location_anchor").after("<%=j render 'settings/user_location' %>");
  }
<% end %>
