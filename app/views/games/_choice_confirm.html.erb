<div id="confirm_choice_<%= turn_choice %>" hidden>

  <%= link_to confirm_turn_choice_path(turn_choice), remote: true do %>
  
    <div class="choice_confirm_btn">
      Confirm
    </div>
    
  <% end %>
  
</div>
